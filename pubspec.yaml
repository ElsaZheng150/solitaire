name: solitaire
description: "A single player card game also known as Klondike"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.1
  flutter: ^3.13.0

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.38.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/


